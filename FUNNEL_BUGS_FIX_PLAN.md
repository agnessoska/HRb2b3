# План исправления багов воронки найма

**Дата:** 09.12.2025  
**Сессия:** #80  
**Связано с:** Сессия #79 (Реализация умной воронки)

---

## 🐛 Выявленные проблемы

### Проблема 1: Отсутствие подтверждающих диалогов перед AI-генерацией

**Файл:** `src/features/vacancy-management/ui/funnel/VacancyFunnel.tsx`

**Что не так:**
- Кнопка "Полный анализ" (функция `handleGenerateAnalysis`, строка 207) сразу запускает генерацию
- Кнопка "Пригласить на интервью" (функция `handleInviteToInterview`, строка 244) также генерирует без подтверждения
- Пользователь не может выбрать язык результата
- Пользователь не видит примерную стоимость
- Нет описания что делает функция

**Ожидаемое поведение:**
- Клик по кнопке → открывается диалог с описанием, выбором параметров и информацией о стоимости
- После подтверждения → показывается `AIGenerationModal` с прогрессом
- После завершения → результат открывается в overlay

---

### Проблема 2: Accessibility warnings в консоли

**Файл:** `src/features/vacancy-management/ui/funnel/VacancyFunnel.tsx` (строки 545-551)

**Ошибки Radix UI:**
```
DialogContent requires a DialogTitle for screen reader users
Missing Description or aria-describedby for DialogContent
```

**Что не так:**
В overlay для интервью отсутствуют обязательные элементы DialogHeader, DialogTitle, DialogDescription

**Требование WCAG 2.1:** Все модальные окна должны иметь заголовок и описание для screen readers

---

### Проблема 3: Отсутствие скролла в overlay интервью

**Файл:** `src/features/vacancy-management/ui/funnel/VacancyFunnel.tsx` (строка 547)

**Что не так:**
- `overflow-hidden` → контент обрезается, нет возможности прокрутить
- `p-0` → убран padding, но InterviewWorkspace не адаптирован
- `flex flex-col` → не нужен (DialogContent сам flex контейнер)

**Ожидаемое поведение:**
- Вертикальный скролл для длинного контента
- Стандартный padding для визуального комфорта

---

## 🔧 План исправлений

### Исправление 1: Управляемые диалоги подтверждения

#### Шаг 1.1: Обновить GenerateFullAnalysisDialog.tsx

Сделать компонент управляемым (controlled) для программного открытия из воронки.

**Новые пропсы:**
- `isOpen?: boolean` - внешнее управление состоянием
- `onOpenChange?: (open: boolean) => void` - callback для изменения состояния
- `children?: React.ReactNode` - опциональный trigger (для обратной совместимости)

**Логика:**
- Если `isOpen` передан → controlled режим (управление снаружи)
- Если `isOpen` не передан → uncontrolled режим (внутренний state)
- Если `children` передан → рендерить DialogTrigger

#### Шаг 1.2: Обновить GenerateStructuredInterviewDialog.tsx

Применить аналогичный паттерн controlled/uncontrolled.

#### Шаг 1.3: Обновить VacancyFunnel.tsx

**Добавить состояния для диалогов:**
```typescript
const [analysisDialogState, setAnalysisDialogState] = useState<{
  isOpen: boolean;
  applicationId: string | null;
}>({ isOpen: false, applicationId: null });

const [interviewDialogState, setInterviewDialogState] = useState<{
  isOpen: boolean;
  applicationId: string | null;
}>({ isOpen: false, applicationId: null });
```

**Изменить обработчики:**
- `handleGenerateAnalysis`: вместо немедленной генерации → открыть диалог
- `handleInviteToInterview`: вместо немедленной генерации → открыть диалог

**Добавить новые обработчики для подтвержденной генерации:**
- `handleConfirmedAnalysisGeneration` - вызывается после подтверждения в диалоге
- `handleConfirmedInterviewGeneration` - вызывается после подтверждения в диалоге

**Рендерить управляемые диалоги:**
- Условный рендер `GenerateFullAnalysisDialog` с передачей `isOpen` и callback
- Условный рендер `GenerateStructuredInterviewDialog` с передачей `isOpen` и callback

---

### Исправление 2: Accessibility в overlay окнах

#### Шаг 2.1: Исправить overlay интервью

**Добавить в VacancyFunnel.tsx (строка 546):**
- `DialogHeader` с заголовком и описанием
- Заменить `overflow-hidden` на `overflow-y-auto`
- Убрать `flex flex-col` и `p-0`

#### Шаг 2.2: Исправить overlay анализа

**Добавить в VacancyFunnel.tsx (строка 537):**
- `DialogDescription` к существующему `DialogHeader`

---

### Исправление 3: Адаптация InterviewWorkspace для диалога

#### Шаг 3.1: Добавить режим диалога

**В InterviewWorkspace.tsx:**
- Добавить проп `isInDialog?: boolean`
- Условно применять sticky positioning (только если НЕ в диалоге)
- Скрыть кнопку "Назад" в режиме диалога (она не нужна)
- Убрать отрицательные margin `-mx-6` в режиме диалога

#### Шаг 3.2: Передать флаг из VacancyFunnel

В `InterviewWorkspaceWrapper` передать `isInDialog={true}` при вызове `InterviewWorkspace`

---

## 📝 Локализация

### Файлы для обновления:

**interview.json (3 файла - ru, en, kk):**
```json
{
  "workspace": {
    "title": "...",
    "description": "..."
  }
}
```

**ai-analysis.json (3 файла - ru, en, kk):**
```json
{
  "fullAnalysis": {
    "viewDescription": "..."
  }
}
```

**Версия кэша в i18n.ts:**
Обновить с `20251209-smart-funnel` на `20251209-bugfix`

---

## 📊 Итоговая статистика

**Файлы к изменению:** 10 файлов
- 4 файла компонентов
- 6 файлов локализации

**Новый код:** ~165 строк  
**Изменения:** ~40 строк  
**Время реализации:** ~2 часа

---

## ✅ Критерии приемки

После исправления:

1. ✅ Клик "Полный анализ" в воронке → диалог с выбором вакансий, языка, информацией о стоимости
2. ✅ Клик "Пригласить на интервью" → диалог с выбором вакансии, языка, полем для заметок
3. ✅ После подтверждения → AIGenerationModal с прогрессом
4. ✅ После генерации → overlay с результатом
5. ✅ Нет warnings accessibility в консоли
6. ✅ Overlay интервью корректно скроллится
7. ✅ Все элементы доступны для screen readers
8. ✅ Полная локализация на ru/en/kk

---

## 🚀 Готовность к реализации

План детально проработан. Все изменения локализованы и не затрагивают бизнес-логику.

**Следующий шаг:** Переключение в Code mode для реализации