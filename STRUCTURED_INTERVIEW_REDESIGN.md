# Полная переработка модуля "Структурированное интервью"

**Дата:** 9 декабря 2025  
**Статус:** Planning  
**Цель:** Превратить статичный документ в интерактивный рабочий инструмент для проведения интервью

---

## 🎯 Видение

Структурированное интервью должно быть **живым инструментом**, который помогает HR проводить профессиональное интервью, а не просто документом для чтения.

### "До" (текущее состояние)
- ❌ Статичный Markdown документ
- ❌ Нет интерактивности
- ❌ Невозможно делать заметки во время интервью
- ❌ Нет контроля времени
- ❌ Нет понимания, проводилось ли интервью
- ❌ PDF через html2canvas (медленно, ненадежно)

### "После" (целевое состояние)
- ✅ **Интерактивный workspace** с секциями
- ✅ **Чекбоксы** для отметки пройденных блоков
- ✅ **Поля для заметок** под каждым вопросом
- ✅ **Таймер** для контроля времени секций
- ✅ **Система оценок** (1-5 звезд) для ключевых ответов
- ✅ **Автосохранение** прогресса в localStorage
- ✅ **История** проведенных интервью
- ✅ **Качественный PDF** через React-PDF (векторный)
- ✅ **Финальный протокол** со всеми заметками и оценками

---

## 📊 Архитектура данных

### Новая таблица: `interview_sessions`

```sql
CREATE TABLE public.interview_sessions (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now(),
  
  -- Relations
  organization_id uuid REFERENCES organizations(id) NOT NULL,
  hr_specialist_id uuid REFERENCES hr_specialists(id) NOT NULL,
  candidate_id uuid REFERENCES candidates(id) NOT NULL,
  vacancy_id uuid REFERENCES vacancies(id) NOT NULL,
  
  -- Interview Data
  interview_plan jsonb NOT NULL,  -- Структура плана от AI
  session_data jsonb,              -- Заметки, оценки, отметки завершения
  
  -- Status
  status text NOT NULL DEFAULT 'planned',  -- planned, in_progress, completed, cancelled
  started_at timestamptz,
  completed_at timestamptz,
  duration_minutes integer,  -- Фактическая продолжительность
  
  -- Metadata
  language text NOT NULL DEFAULT 'ru',
  version text DEFAULT 'v3'
);

-- Indexes
CREATE INDEX idx_interview_sessions_candidate ON interview_sessions(candidate_id);
CREATE INDEX idx_interview_sessions_hr ON interview_sessions(hr_specialist_id);
CREATE INDEX idx_interview_sessions_org ON interview_sessions(organization_id);
CREATE INDEX idx_interview_sessions_status ON interview_sessions(status);

-- RLS Policies
CREATE POLICY "hr_can_view_own_interviews"
ON interview_sessions FOR SELECT
TO authenticated
USING (
  hr_specialist_id IN (
    SELECT id FROM hr_specialists WHERE user_id = auth.uid()
  )
);

CREATE POLICY "hr_can_manage_own_interviews"
ON interview_sessions FOR ALL
TO authenticated
USING (
  hr_specialist_id IN (
    SELECT id FROM hr_specialists WHERE user_id = auth.uid()
  )
);
```

### Структура `interview_plan` (JSONB)

```json
{
  "candidate_name": "Иван Иванов",
  "vacancy_title": "Senior Frontend Developer",
  "generated_at": "2025-12-09T10:00:00Z",
  "estimated_duration": 60,
  "sections": [
    {
      "id": "intro",
      "title": "Введение",
      "time_allocation": 5,
      "items": [
        {
          "type": "script",
          "content": "Добрый день! Спасибо что пришли..."
        },
        {
          "type": "ice_breaker",
          "question": "Расскажите, как вы узнали о нашей компании?",
          "notes_placeholder": "Первое впечатление..."
        }
      ]
    },
    {
      "id": "experience",
      "title": "Проверка опыта",
      "time_allocation": 20,
      "items": [
        {
          "type": "question",
          "category": "technical",
          "question": "Опишите самый сложный проект, над которым вы работали с React...",
          "what_to_listen_for": "Глубина понимания архитектуры, умение решать проблемы",
          "red_flags": ["Поверхностные ответы", "Отсутствие конкретики"],
          "rating_enabled": true
        }
      ]
    },
    {
      "id": "psychometric",
      "title": "Психометрическая валидация",
      "time_allocation": 15,
      "focus": "критически_важно",
      "items": [
        {
          "type": "risk_check",
          "risk": "Высокий нейротизм (72/100) - может испытывать стресс",
          "question": "Расскажите о ситуации, когда вы работали под сильным давлением...",
          "what_to_listen_for": "Стратегии управления стрессом",
          "rating_enabled": true
        }
      ]
    },
    {
      "id": "motivation",
      "title": "Мотивация и культура",
      "time_allocation": 10,
      "items": [...]
    },
    {
      "id": "conclusion",
      "title": "Заключение",
      "time_allocation": 5,
      "items": [...]
    }
  ]
}
```

### Структура `session_data` (JSONB)

```json
{
  "progress": {
    "current_section": "experience",
    "sections_completed": ["intro"],
    "total_progress_percent": 20
  },
  "notes": {
    "intro-0": "Кандидат очень заинтересован, узнал через LinkedIn",
    "experience-0": "Отличное знание React, работал над сложным state management..."
  },
  "ratings": {
    "experience-0": 5,
    "psychometric-0": 4
  },
  "section_times": {
    "intro": 4,
    "experience": 18
  },
  "overall_impression": "Отличный кандидат, рекомендую нанимать"
}
```

---

## 🎨 UI/UX Дизайн

### 1. Режим "Подготовка" (до интервью)

**Layout:**
```
┌─────────────────────────────────────────────┐
│ [← Назад]        Интервью: Иван Иванов      │
│                                              │
│ ┌──────────────────────────────────────────┐│
│ │  GlassCard - Информация                  ││
│ │  📅 Запланировано: 10 дек, 14:00         ││
│ │  ⏱️  Продолжительность: ~60 минут        ││
│ │  💼 Вакансия: Senior Frontend Developer  ││
│ │                                           ││
│ │  [Начать интервью] [Скачать PDF]         ││
│ └──────────────────────────────────────────┘│
│                                              │
│ ┌──────────────────────────────────────────┐│
│ │ Accordion Section 1: Введение (5 мин)    ││
│ │   • Ice breaker вопрос...                ││
│ │   • Обзор процесса...                    ││
│ └──────────────────────────────────────────┘│
│ ┌──────────────────────────────────────────┐│
│ │ Accordion Section 2: Опыт (20 мин)       ││
│ │   • Вопрос 1: [текст]                    ││
│ │   • Вопрос 2: [текст]                    ││
│ └──────────────────────────────────────────┘│
└─────────────────────────────────────────────┘
```

### 2. Режим "Проведение" (во время интервью)

**Layout:**
```
┌─────────────────────────────────────────────┐
│ Sticky Header                                │
│ Progress: [████████░░░░░░░░] 40%           │
│ Время: 24:15 / ~60:00    [⏸️ Пауза] [✓ Завершить]│
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ ✓ 1. Введение (5 мин) - Завершено          │
│ ▶ 2. Проверка опыта (20 мин) - Текущий     │
│   3. Психометрия (15 мин)                   │
│   4. Мотивация (10 мин)                     │
│   5. Заключение (5 мин)                     │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Card - Текущая секция                        │
│                                              │
│ ## Вопрос 1 (STAR метод)                    │
│ "Опишите ситуацию, когда вам пришлось..."  │
│                                              │
│ 💡 Что искать: глубина понимания, примеры   │
│ 🚩 Red flags: поверхностные ответы          │
│                                              │
│ Оценка: ⭐⭐⭐⭐⭐ (кликабельные звезды)       │
│                                              │
│ 📝 Заметки:                                  │
│ ┌─────────────────────────────────────────┐ │
│ │ [Текстовое поле для заметок]            │ │
│ │                                         │ │
│ └─────────────────────────────────────────┘ │
│                                              │
│ [☑️ Отметить как пройденный]                │
└─────────────────────────────────────────────┘
```

### 3. После завершения

**Layout:**
```
┌─────────────────────────────────────────────┐
│ AIBorder + GlassCard                         │
│ ✅ Интервью завершено                        │
│ Продолжительность: 58 минут                 │
│ Дата: 10 декабря 2025, 14:00               │
│                                              │
│ [Просмотреть протокол] [Скачать PDF]       │
└─────────────────────────────────────────────┘

┌─────────────────────────────────────────────┐
│ Протокол интервью (читаемый режим)          │
│                                              │
│ ### 1. Введение ✓                           │
│ ⏱️ Время: 4 минуты                          │
│ 📝 "Кандидат пришел вовремя, дружелюбный..." │
│                                              │
│ ### 2. Проверка опыта ✓                     │
│ ⏱️ Время: 18 минут                          │
│                                              │
│ **Вопрос 1:** [текст вопроса]               │
│ ⭐ Оценка: 5/5                              │
│ 📝 "Отличный пример с микрофронтендами..."  │
│                                              │
│ ### Итоговое впечатление                    │
│ 📝 "Сильный кандидат, рекомендую hiring..." │
└─────────────────────────────────────────────┘
```

---

## 🏗️ Техническая архитектура

### Компоненты

**1. `InterviewWorkspace.tsx`** (главный компонент)
- Управляет состоянием всей сессии
- Переключает режимы (preview/active/completed)
- Обрабатывает автосохранение

**2. `InterviewPreview.tsx`**
- Отображение плана до начала (readonly)
- Accordion с секциями
- Кнопки "Начать" и "Скачать"

**3. `ActiveInterview.tsx`**
- Интерактивный режим проведения
- Sticky header с прогрессом и таймером
- Навигация по секциям

**4. `InterviewSection.tsx`**
- Отдельная секция интервью
- Чекбокс завершения
- Таймер секции

**5. `InterviewQuestion.tsx`**
- Вопрос с подсказками
- Star rating
- Текстовое поле заметок
- Автосохранение

**6. `InterviewProtocol.tsx`**
- Финальный читаемый протокол
- Все заметки и оценки
- Время каждой секции

**7. `InterviewTimer.tsx`**
- Общий таймер + таймер секции
- Play/Pause
- Visual indicator

**8. `InterviewHistory.tsx`**
- Список всех интервью кандидата
- Фильтры (Completed/Cancelled)
- Быстрый доступ к протоколам

**9. `InterviewPDF.tsx`**
- React-PDF Document компонент
- Красивая верстка протокола
- Поддержка кириллицы

---

## 🔄 User Flow

### Сценарий 1: Генерация интервью

1. HR открывает профиль кандидата
2. Нажимает "Сгенерировать план интервью"
3. Выбирает вакансию, язык, добавляет акценты
4. AI генерирует структурированный план (JSON)
5. Сохраняется в `interview_sessions` со статусом `planned`
6. HR видит список интервью в истории

### Сценарий 2: Подготовка

1. HR открывает запланированное интервью из истории
2. Видит режим "Подготовка" с полным планом
3. Изучает секции, вопросы, психометрические подсказки
4. Скачивает PDF для печати (опционально)

### Сценарий 3: Проведение интервью

1. HR нажимает "Начать интервью"
2. Открывается интерактивный режим:
   - Запускается общий таймер
   - Активируется первая секция
3. Для каждого вопроса HR:
   - Читает вопрос кандидату
   - Слушает ответ, смотрит подсказки
   - Ставит оценку (звезды)
   - Пишет заметку
   - Отмечает как пройденный
4. Переходит к следующей секции
5. Данные автосохраняются каждые 30 секунд в localStorage
6. При переходе между секциями - сохранение в БД

### Сценарий 4: Завершение

1. HR нажимает "Завершить интервью"
2. Открывается модальное окно:
   - "Итоговое впечатление" (textarea)
   - "Рекомендация" (hire/consider/reject)
3. Подтверждение → сохранение
4. Статус меняется на `completed`
5. Показывается финальный протокол
6. Доступен экспорт в PDF

---

## 🎨 Дизайн компонентов

### Sticky Progress Header (во время интервью)

```tsx
<div className="sticky top-16 z-40 bg-background/95 backdrop-blur border-b shadow-sm">
  <div className="container py-3">
    <div className="flex items-center justify-between gap-4">
      {/* Progress bar */}
      <div className="flex-1">
        <div className="flex items-center gap-3 mb-2">
          <span className="text-sm font-medium">Прогресс:</span>
          <span className="text-sm text-muted-foreground">2/5 секций</span>
        </div>
        <Progress value={40} className="h-2" />
      </div>
      
      {/* Timer */}
      <div className="flex items-center gap-2 bg-muted/50 px-4 py-2 rounded-lg">
        <Clock className="h-4 w-4" />
        <span className="font-mono text-lg">24:15</span>
        <span className="text-muted-foreground">/ 60:00</span>
      </div>
      
      {/* Actions */}
      <Button variant="ghost" size="sm">
        <Pause className="h-4 w-4" />
      </Button>
      <Button variant="default" size="sm">
        <Check className="h-4 w-4" />
        Завершить
      </Button>
    </div>
  </div>
</div>
```

### Section Navigator (левый sidebar на desktop)

```tsx
<div className="space-y-2">
  {sections.map((section, idx) => (
    <button
      className={cn(
        "w-full text-left p-3 rounded-lg border transition-all",
        section.completed && "bg-emerald-50 border-emerald-200",
        section.active && "bg-primary/10 border-primary ring-2 ring-primary/20",
        !section.active && !section.completed && "hover:bg-muted"
      )}
    >
      <div className="flex items-center gap-3">
        {section.completed ? (
          <Check className="h-5 w-5 text-emerald-600" />
        ) : section.active ? (
          <div className="h-5 w-5 rounded-full bg-primary animate-pulse" />
        ) : (
          <div className="h-5 w-5 rounded-full border-2" />
        )}
        <div className="flex-1">
          <div className="font-medium">{idx + 1}. {section.title}</div>
          <div className="text-xs text-muted-foreground">
            {section.time_allocation} минут
          </div>
        </div>
      </div>
    </button>
  ))}
</div>
```

### Question Card (интерактивный)

```tsx
<Card className={cn("transition-all", isAnswered && "border-emerald-200")}>
  <CardHeader>
    <div className="flex items-start justify-between gap-4">
      <div className="flex-1">
        <CardTitle className="text-lg">{question.question}</CardTitle>
        {question.category && (
          <Badge variant="outline" className="mt-2">
            {question.category}
          </Badge>
        )}
      </div>
      <Checkbox 
        checked={isAnswered}
        onCheckedChange={handleToggle}
      />
    </div>
  </CardHeader>
  
  <CardContent className="space-y-4">
    {/* Подсказки */}
    {question.what_to_listen_for && (
      <div className="flex gap-2 p-3 bg-blue-50 dark:bg-blue-950/20 rounded-lg">
        <Lightbulb className="h-5 w-5 text-blue-600 shrink-0" />
        <div className="text-sm">
          <strong>Что искать:</strong> {question.what_to_listen_for}
        </div>
      </div>
    )}
    
    {question.red_flags && (
      <div className="flex gap-2 p-3 bg-red-50 dark:bg-red-950/20 rounded-lg">
        <AlertTriangle className="h-5 w-5 text-red-600 shrink-0" />
        <div className="text-sm">
          <strong>Red flags:</strong>
          <ul className="list-disc list-inside mt-1">
            {question.red_flags.map(flag => <li key={flag}>{flag}</li>)}
          </ul>
        </div>
      </div>
    )}
    
    {/* Rating */}
    {question.rating_enabled && (
      <div className="space-y-2">
        <Label>Оценка ответа:</Label>
        <div className="flex gap-1">
          {[1,2,3,4,5].map(star => (
            <button
              onClick={() => handleRate(star)}
              className="transition-transform hover:scale-110"
            >
              <Star 
                className={cn(
                  "h-6 w-6",
                  rating >= star ? "fill-amber-400 text-amber-400" : "text-gray-300"
                )}
              />
            </button>
          ))}
        </div>
      </div>
    )}
    
    {/* Notes */}
    <div className="space-y-2">
      <Label>Заметки:</Label>
      <Textarea
        value={notes}
        onChange={(e) => handleNotesChange(e.target.value)}
        placeholder="Запишите ответ кандидата, ваши наблюдения..."
        className="min-h-[100px]"
      />
    </div>
  </CardContent>
</Card>
```

### Final Protocol (после завершения)

```tsx
<div className="space-y-6">
  {/* Header */}
  <AIBorder>
    <GlassCard>
      <div className="flex items-center gap-4">
        <div className="h-16 w-16 rounded-2xl bg-gradient-to-br from-emerald-500 to-teal-500 flex items-center justify-center">
          <CheckCircle className="h-8 w-8 text-white" />
        </div>
        <div className="flex-1">
          <h2 className="text-2xl font-bold">Интервью завершено</h2>
          <p className="text-muted-foreground">
            Кандидат: Иван Иванов • Вакансия: Senior Frontend
          </p>
        </div>
        <div className="text-right">
          <div className="text-sm text-muted-foreground">Продолжительность</div>
          <div className="text-2xl font-bold text-emerald-600">58 мин</div>
        </div>
      </div>
    </GlassCard>
  </AIBorder>
  
  {/* Sections */}
  {sections.map(section => (
    <Card>
      <CardHeader>
        <div className="flex items-center gap-3">
          <CheckCircle className="h-5 w-5 text-emerald-600" />
          <CardTitle>{section.title}</CardTitle>
          <Badge variant="outline">{section.time_spent} мин</Badge>
        </div>
      </CardHeader>
      <CardContent className="space-y-4">
        {section.items.map(item => (
          <div className="space-y-2 pb-4 border-b last:border-0">
            <div className="font-medium">{item.question}</div>
            {item.rating && (
              <div className="flex gap-1">
                {Array(item.rating).fill(0).map((_, i) => (
                  <Star key={i} className="h-4 w-4 fill-amber-400 text-amber-400" />
                ))}
              </div>
            )}
            {item.notes && (
              <div className="text-sm text-muted-foreground bg-muted/30 p-3 rounded-lg">
                {item.notes}
              </div>
            )}
          </div>
        ))}
      </CardContent>
    </Card>
  ))}
  
  {/* Overall Impression */}
  <Card>
    <CardHeader>
      <CardTitle>Итоговое впечатление</CardTitle>
    </CardHeader>
    <CardContent>
      <div className="prose prose-sm dark:prose-invert">
        {session.overall_impression}
      </div>
    </CardContent>
  </Card>
</div>
```

---

## 🔧 Технические детали

### Автосохранение

```typescript
// Hook для автосохранения
function useInterviewAutoSave(sessionId: string, sessionData: SessionData) {
  const debouncedData = useDebouncedValue(sessionData, 30000) // 30 sec
  
  useEffect(() => {
    // Save to localStorage
    localStorage.setItem(`interview_${sessionId}`, JSON.stringify(debouncedData))
    
    // Save to DB
    supabase
      .from('interview_sessions')
      .update({ 
        session_data: debouncedData,
        updated_at: new Date().toISOString()
      })
      .eq('id', sessionId)
  }, [debouncedData, sessionId])
}
```

### Таймер

```typescript
function useInterviewTimer(autoStart = false) {
  const [isRunning, setIsRunning] = useState(autoStart)
  const [elapsedSeconds, setElapsedSeconds] = useState(0)
  
  useEffect(() => {
    if (!isRunning) return
    
    const interval = setInterval(() => {
      setElapsedSeconds(prev => prev + 1)
    }, 1000)
    
    return () => clearInterval(interval)
  }, [isRunning])
  
  const formattedTime = useMemo(() => {
    const minutes = Math.floor(elapsedSeconds / 60)
    const seconds = elapsedSeconds % 60
    return `${minutes}:${seconds.toString().padStart(2, '0')}`
  }, [elapsedSeconds])
  
  return {
    isRunning,
    elapsedSeconds,
    formattedTime,
    start: () => setIsRunning(true),
    pause: () => setIsRunning(false),
    reset: () => {
      setIsRunning(false)
      setElapsedSeconds(0)
    }
  }
}
```

### AI Промпт (новая версия v3)

```markdown
Ты - эксперт по проведению структурированных интервью. Создай детальный план интервью в формате JSON.

[Данные кандидата и тестов]

Создай JSON со следующей структурой:

{
  "estimated_duration": 60,
  "sections": [
    {
      "id": "intro",
      "title": "Введение",
      "time_allocation": 5,
      "description": "Установление контакта",
      "items": [
        {
          "type": "script",
          "content": "Добрый день! Спасибо что пришли..."
        },
        {
          "type": "ice_breaker",
          "question": "Как вы узнали о нашей компании?",
          "notes_placeholder": "Первое впечатление..."
        }
      ]
    },
    {
      "id": "experience",
      "title": "Проверка опыта и компетенций",
      "time_allocation": 20,
      "description": "STAR метод для оценки навыков",
      "items": [
        {
          "type": "question",
          "category": "technical",
          "question": "Опишите самый сложный технический вызов...",
          "what_to_listen_for": "Глубина понимания, problem-solving",
          "red_flags": ["Поверхностные ответы", "Нет конкретики"],
          "rating_enabled": true
        }
      ]
    },
    {
      "id": "psychometric",
      "title": "Психометрическая валидация",
      "time_allocation": 15,
      "description": "Проверка рисков и сильных сторон из тестов",
      "focus": "critical",
      "items": [
        {
          "type": "risk_check",
          "risk_description": "Высокий нейротизм (72/100) - возможен стресс",
          "question": "Расскажите о ситуации высокого давления...",
          "what_to_listen_for": "Стратегии coping",
          "rating_enabled": true
        }
      ]
    }
  ]
}

ТРЕБОВАНИЯ:
1. Отвечай ТОЛЬКО валидным JSON
2. Минимум 3 вопроса на секцию "experience"
3. В секции "psychometric" обязательно проверь 1-2 риска из тестов
4. Все вопросы должны быть конкретными, не общими
5. Язык: {language}
```

---

## 📁 Структура файлов

```
src/features/structured-interview/
├── types.ts
├── api/
│   ├── generateInterviewPlan.ts
│   ├── getInterviewSessions.ts
│   ├── updateInterviewSession.ts
│   └── index.ts
├── hooks/
│   ├── useInterviewTimer.ts
│   ├── useInterviewAutoSave.ts
│   └── index.ts
└── ui/
    ├── InterviewWorkspace.tsx          # Main
    ├── InterviewPreview.tsx
    ├── ActiveInterview.tsx
    ├── InterviewSection.tsx
    ├── InterviewQuestion.tsx
    ├── InterviewProtocol.tsx
    ├── InterviewTimer.tsx
    ├── InterviewHistory.tsx
    ├── InterviewCompletionDialog.tsx
    └── pdf/
        └── InterviewProtocolPDF.tsx
```

---

## 🚀 План реализации

### Фаза 1: Backend (База данных и AI)
1. Создать миграцию с таблицей `interview_sessions`
2. Обновить AI промпт до v3 (JSON структура)
3. Переписать Edge Function для новой структуры
4. Создать RPC функции (update_session, complete_session)

### Фаза 2: Core компоненты
1. Создать типы TypeScript
2. Реализовать хуки (timer, autosave)
3. Создать `InterviewWorkspace` с логикой режимов
4. Реализовать `InterviewPreview` (accordion)

### Фаза 3: Интерактив
1. Создать `ActiveInterview` с таймером
2. Реализовать `InterviewSection` с чекбоксами
3. Создать `InterviewQuestion` с rating и notes
4. Добавить `InterviewTimer` компонент
5. Реализовать автосохранение

### Фаза 4: Завершение и протокол
1. Создать `InterviewCompletionDialog`
2. Реализовать `InterviewProtocol` (читаемый формат)
3. Создать `InterviewHistory` для списка
4. Интегрировать в профиль кандидата

### Фаза 5: PDF и финализация
1. Создать `InterviewProtocolPDF` через React-PDF
2. Реализовать экспорт с форматированием
3. Полная локализация (ru/en/kk)
4. Тестирование и полировка

---

## 🎯 Ключевые преимущества нового подхода

### Для HR-специалиста
✅ **Подготовка** - видит план заранее  
✅ **Контроль времени** - не затягивает встречу  
✅ **Структура** - не забывает важные вопросы  
✅ **Документация** - автоматический протокол  
✅ **Качество** - консистентная оценка всех кандидатов  

### Для системы
✅ **Аналитика** - можно анализировать эффективность вопросов  
✅ **Compliance** - все интервью документированы  
✅ **Качество данных** - структурированные заметки и оценки  

### Технические
✅ **Надежность** - автосохранение, защита от потери данных  
✅ **Производительность** - React-PDF вместо html2canvas  
✅ **Масштабируемость** - легко добавить новые типы вопросов  

---

## 💰 Оценка сложности

**Примерное время разработки:** 1 полная рабочая сессия (6-8 часов)

**Сложность компонентов:**
- База данных: 🟢 Простая (1 таблица, 3 RPC)
- AI промпт: 🟢 Простой (текст + JSON структура)
- Edge Function: 🟡 Средняя (парсинг JSON, валидация)
- Frontend: 🔴 Сложная (много интерактивности, state management)
- PDF: 🟡 Средняя (React-PDF уже используется в проекте)

**Риски:**
- ⚠️ Сложность state management (много взаимосвязанных состояний)
- ⚠️ Автосохранение может конфликтовать с быстрыми изменениями
- ⚠️ PDF может быть большим при длинных заметках

**Митигация:**
- Использовать Zustand для глобального state сессии
- Debounce для автосохранения (30 сек)
- Ограничение длины заметок (1000 символов на вопрос)

---

## 🎨 Альтернативные варианты (упрощенные)

### Вариант A: "Минимальный" (быстрая реализация)
- Убрать таймер и звезды
- Только чекбоксы и заметки
- Простой протокол в конце
- **Плюс:** Быстро (3-4 часа)
- **Минус:** Меньше пользы для HR

### Вариант B: "Гибридный" (рекомендуемый)
- Оставить таймер, убрать звезды
- Чекбоксы + заметки
- Красивый протокол + PDF
- **Плюс:** Баланс функциональности и времени
- **Минус:** Нет количественной оценки ответов

### Вариант C: "Полный" (как описано выше)
- Все фичи: таймер, звезды, чекбоксы, заметки
- Продвинутый протокол
- React-PDF с отличным дизайном
- **Плюс:** Максимальная польза
- **Минус:** Дольше разработка

---

## ❓ Вопросы для обсуждения

1. **Какой вариант реализации предпочтительнее?** (A/B/C)
2. **Нужна ли система оценок (звезды)?** Или достаточно заметок?
3. **Таймер обязателен?** Или это "nice to have"?
4. **История интервью** - где лучше показывать: на странице кандидата или отдельная вкладка?
5. **Отмена интервью** - нужна ли возможность прервать и вернуться позже?

---

## 📝 Рекомендация

Я рекомендую **Вариант B (Гибридный)** как оптимальный баланс:

**Что включить (Must Have):**
- ✅ Интерактивные чекбоксы
- ✅ Поля для заметок с автосохранением
- ✅ Таймер (общий + по секциям)
- ✅ Финальный протокол
- ✅ React-PDF экспорт
- ✅ История интервью

**Что оставить на потом (Nice to Have):**
- ⏸️ Звездочки для оценок (можно добавить позже)
- ⏸️ Аналитика по вопросам
- ⏸️ Сравнение разных интервью

Это даст нам **90% пользы при 60% усилий**.

---

## 🎬 Следующий шаг

После твоего одобрения плана и выбора варианта, я переключусь в **Code режим** и начну реализацию:

1. Создам миграцию БД
2. Обновлю AI промпт
3. Перепишу Edge Function
4. Создам все компоненты
5. Интегрирую в существующий UI
6. Добавлю локализацию

**Готов приступить!** Какой вариант выбираешь? 🚀