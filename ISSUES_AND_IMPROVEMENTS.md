# Список проблем и улучшений HR Platform v2.0

**Дата анализа:** 17 ноября 2025
**Статус проекта:** Черновая версия, требует значительной доработки

---

## 📊 ОБЩАЯ СТАТИСТИКА

- **Всего файлов TypeScript:** 150+
- **Критических проблем:** 8
- **Важных проблем:** 24
- **Мелких проблем:** 18
- **Общее время на доработку:** 4-7 недель

---

## 🔴 КРИТИЧЕСКИЕ ПРОБЛЕМЫ (Блокирующие продакшн)

### 1. Навигация и Header полностью отсутствуют
**Файл:** `src/shared/ui/layouts/DashboardLayout.tsx`

**Проблема:**
- Нет навигационного меню для перехода между разделами
- Нет логотипа организации (строка 18: TODO)
- Нет отображения имени организации
- Нет баланса токенов
- Нет меню пользователя (строка 25: TODO)
- Нет кнопки выхода
- Нет dropdown меню профиля

**Что нужно:**
```tsx
Header должен содержать:
├── Левая часть:
│   ├── Логотип организации (из organization.brand_logo_url)
│   └── Название организации (из organization.name)
├── Центральная часть (для HR):
│   ├── Ссылка "Дашборд"
│   ├── Ссылка "Вакансии"
│   ├── Ссылка "Кандидаты"
│   ├── Ссылка "Рынок талантов"
│   ├── Ссылка "Чат"
│   ├── Ссылка "Команда"
│   └── Ссылка "Биллинг"
└── Правая часть:
    ├── Баланс токенов (organization.token_balance)
    ├── Кнопка "Купить токены"
    ├── Переключатель языка
    ├── Переключатель темы
    └── Dropdown меню пользователя:
        ├── Имя пользователя
        ├── Email
        ├── Настройки
        └── Выход
```

**Приоритет:** КРИТИЧЕСКИЙ

---

### 2. Страница управления командой - заглушка
**Файл:** `src/pages/hr/team/index.tsx`

**Проблема:**
Страница содержит только текст "This page is under construction". Полностью отсутствует функционал.

**Что нужно реализовать:**
1. Список всех HR-специалистов организации (из таблицы `hr_specialists`)
2. Кнопка "Пригласить сотрудника"
3. Диалог приглашения с полями:
   - Email нового сотрудника
   - Роль (owner/member)
4. Возможность деактивировать сотрудника (is_active = false)
5. Отображение статуса каждого сотрудника (активен/неактивен)
6. Фильтр по статусу

**Файлы для создания:**
- `src/features/team-management/api/getTeamMembers.ts`
- `src/features/team-management/api/inviteTeamMember.ts`
- `src/features/team-management/api/updateTeamMember.ts`
- `src/features/team-management/ui/TeamMemberList.tsx`
- `src/features/team-management/ui/InviteTeamMemberDialog.tsx`
- `src/features/team-management/ui/TeamMemberCard.tsx`

**Приоритет:** КРИТИЧЕСКИЙ

---

### 3. Отсутствуют страницы ошибок
**Проблема:**
Нет обработки 404, 500 и других ошибок.

**Что нужно создать:**
1. `src/pages/errors/404.tsx` - страница "Не найдено"
2. `src/pages/errors/500.tsx` - страница "Ошибка сервера"
3. `src/app/providers/ErrorBoundary.tsx` - React Error Boundary
4. Обновить роутер для обработки несуществующих маршрутов

**Приоритет:** КРИТИЧЕСКИЙ

---

### 4. Типы базы данных некорректные
**Файл:** `src/shared/types/database.ts`

**Проблема:**
Файл содержит некорректные символы в начале, может вызывать ошибки типизации.

**Что нужно:**
```bash
# Регенерировать типы из Supabase
npx supabase login
npx supabase gen types typescript --project-id <project-id> > src/shared/types/database.ts
```

**Приоритет:** КРИТИЧЕСКИЙ

---

### 5. Отсутствует .env.example
**Проблема:**
Нет примера файла с переменными окружения.

**Что нужно создать:**
```env
# .env.example

# Supabase
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-anon-key

# Для Edge Functions (устанавливаются в Supabase Dashboard)
# ANTHROPIC_API_KEY=your-anthropic-key
# GOOGLE_AI_API_KEY=your-google-key (если используете Gemini)
# ROBOKASSA_MERCHANT_LOGIN=your-merchant-login
# ROBOKASSA_PASSWORD_1=your-password-1
# ROBOKASSA_PASSWORD_2=your-password-2
```

**Приоритет:** КРИТИЧЕСКИЙ

---

### 6. Нет страницы регистрации по инвайт-токену
**Проблема:**
Отсутствует маршрут и страница для регистрации кандидатов по токену приглашения.

**Что нужно создать:**
1. Маршрут: `/auth/register/:token`
2. Страница: `src/pages/auth/register-by-token/index.tsx`
3. Компонент: `src/features/auth/ui/RegisterByTokenForm.tsx`
4. API функция: `src/features/auth/api/registerByToken.ts`

**Логика:**
1. Проверить токен в таблице `invitation_tokens`
2. Если токен валиден и не использован:
   - Показать форму регистрации
   - При успешной регистрации:
     - Создать пользователя в auth.users
     - Создать запись в candidates
     - Установить invited_by_hr_id и invited_by_organization_id
     - Пометить токен как использованный

**Приоритет:** КРИТИЧЕСКИЙ

---

### 7. RLS политики не проверены
**Проблема:**
Нет уверенности, что все Row Level Security политики работают корректно.

**Что нужно:**
1. Провести полный аудит RLS политик
2. Протестировать каждую политику:
   - HR может видеть только своих кандидатов
   - Кандидат видит только свои данные
   - Нет утечек данных между организациями
3. Проверить политики на таблицах:
   - organizations
   - hr_specialists
   - candidates
   - vacancies
   - applications
   - candidate_test_results
   - chat_messages

**Приоритет:** КРИТИЧЕСКИЙ

---

### 8. Отсутствует функционал редактирования профиля кандидата
**Проблема:**
Кандидаты не могут редактировать свой профиль (опыт, образование, навыки, контакты).

**Что нужно создать:**
1. Страница: `src/pages/candidate/profile/index.tsx`
2. Компонент: `src/features/candidate-management/ui/EditCandidateProfileDialog.tsx`
3. API функция: `src/features/candidate-management/api/updateCandidateProfile.ts`

**Форма должна содержать:**
- ФИО
- Телефон
- Email (readonly, из auth.users)
- Категория профессии (select из professional_categories)
- Опыт работы (textarea)
- Образование (textarea)
- О себе (textarea)
- Навыки (multiselect с автокомплитом из skills_dictionary)
- Чекбокс "Показывать в рынке талантов" (is_public)

**Приоритет:** КРИТИЧЕСКИЙ

---

## 🟠 ВАЖНЫЕ ПРОБЛЕМЫ (Влияют на UX)

### 9. Нерабочие кнопки в списке вакансий
**Файл:** `src/features/vacancy-management/ui/VacancyList.tsx`

**Проблема:**
- Кнопка "Edit" (строка 64) - только console.log
- Кнопка "Archive" (строка 69) - только console.log

**Что нужно:**
1. Создать `EditVacancyDialog.tsx` с формой редактирования
2. Создать API функцию `updateVacancy.ts`
3. Создать API функцию `archiveVacancy.ts`
4. Подключить обработчики к кнопкам

**Приоритет:** ВАЖНЫЙ

---

### 10. Проблемы в профиле кандидата
**Файл:** `src/pages/hr/candidate/profile/index.tsx`

**Проблемы:**
1. **Строка 173:** TODO - передается неправильный vacancy_id для генерации интервью
2. **Строка 190:** Email отображает ID кандидата вместо email
3. **Строки 95-98:** Кнопка "Share" не работает
4. **Строки 99-102:** Кнопка "Download PDF" не работает
5. **Строка 140:** Кнопка "View" для документов не работает

**Что нужно:**
1. Добавить в API getCandidateById получение email из auth.users
2. Добавить диалог выбора вакансии для генерации интервью
3. Реализовать функцию шаринга (копирование ссылки или email)
4. Реализовать экспорт анализа в PDF (используя jsPDF)
5. Реализовать просмотр документов (модальное окно с markdown)

**Приоритет:** ВАЖНЫЙ

---

### 11. Нерабочие кнопки на странице результатов теста
**Файл:** `src/pages/candidate/test/results/index.tsx`

**Проблема:**
- Кнопка "Download PDF" (строки 113-116) не работает
- Кнопка "Retake Test" (строка 117) не работает

**Что нужно:**
1. Реализовать экспорт результатов в PDF
2. Реализовать пересдачу теста с проверкой cooldown периода
3. Проверить поле `retake_available_at` перед разрешением пересдачи

**Приоритет:** ВАЖНЫЙ

---

### 12. Отсутствует отображение категории профессии
**Файл:** `src/features/candidate-management/ui/CandidateCard.tsx`

**Проблема:**
TODO на строке 48 - нужно получать название категории из таблицы professional_categories.

**Что нужно:**
1. Обновить API `getCandidates` для join с professional_categories
2. Добавить в типы поле category_name
3. Отобразить категорию в карточке

**Приоритет:** ВАЖНЫЙ

---

### 13. Не заполнены справочники
**Проблема:**
Отсутствуют данные в справочных таблицах:
- `professional_categories` - пустая
- `skills_dictionary` - пустая или недостаточно данных

**Что нужно создать:**
1. **Миграция для категорий профессий:**
   ```sql
   -- supabase/migrations/00XX_populate_professional_categories.sql
   INSERT INTO professional_categories (name_ru, name_kk, name_en, sort_order) VALUES
   ('IT и технологии', 'IT және технологиялар', 'IT and Technology', 1),
   ('Финансы и банковское дело', 'Қаржы және банк ісі', 'Finance and Banking', 2),
   ('Маркетинг и продажи', 'Маркетинг және сату', 'Marketing and Sales', 3),
   -- ... еще 20-30 категорий
   ```

2. **Миграция для навыков:**
   ```sql
   -- supabase/migrations/00XX_populate_skills_dictionary.sql
   -- ~5000+ записей навыков на 3 языках
   ```

**Приоритет:** ВАЖНЫЙ

---

### 14. Отсутствуют toast-уведомления
**Файлы:**
- `src/pages/hr/vacancy/profile/index.tsx` (строки 36, 39)
- `src/features/ai-analysis/ui/GenerateFullAnalysisDialog.tsx` (строки 39, 43)
- `src/features/candidate-management/ui/GenerateInviteLinkDialog.tsx` (строки 39-40, 52)

**Проблема:**
Вместо TODO нужны реальные уведомления об успехе/ошибке.

**Что нужно:**
Библиотека `sonner` уже установлена, нужно просто заменить TODO на:
```tsx
import { toast } from 'sonner'

// Success
toast.success(t('success_message'))

// Error
toast.error(t('error_message'))
```

**Приоритет:** ВАЖНЫЙ

---

### 15. Hardcoded язык в результатах теста
**Файл:** `src/pages/candidate/test/results/index.tsx`

**Проблема:**
- Строка 124: Используется `test.name_ru` вместо динамического языка
- Строки 19, 128: Используется русская локаль для дат

**Что нужно:**
```tsx
import { useTranslation } from 'react-i18next'

const { i18n } = useTranslation()
const currentLang = i18n.language // 'ru' | 'en' | 'kk'

// Для названия теста
const testName = test[`name_${currentLang}`]

// Для дат
import { format } from 'date-fns'
import { ru, enUS, kk } from 'date-fns/locale'

const locales = { ru, en: enUS, kk }
format(new Date(), 'PPP', { locale: locales[currentLang] })
```

**Приоритет:** ВАЖНЫЙ

---

### 16. Нет валидации форм
**Проблема:**
Не все формы имеют комплексную валидацию полей.

**Что нужно проверить и дополнить:**
1. Форма создания вакансии
2. Форма регистрации
3. Форма редактирования профиля кандидата
4. Форма приглашения в команду

**Пример валидации:**
```tsx
import { z } from 'zod'

const schema = z.object({
  email: z.string().email('Некорректный email'),
  phone: z.string().regex(/^\+?\d{10,15}$/, 'Некорректный телефон'),
  // ...
})
```

**Приоритет:** ВАЖНЫЙ

---

### 17. Отсутствует мобильная адаптация
**Проблема:**
Проект не тестировался на мобильных устройствах.

**Что нужно:**
1. Проверить все страницы на разных разрешениях:
   - Mobile (320px - 480px)
   - Tablet (481px - 768px)
   - Desktop (769px+)
2. Особое внимание:
   - Таблицы (должны скроллиться или карточками)
   - Навигация (должна быть burger menu на мобилке)
   - Формы (удобный ввод на тач-устройствах)
   - Диалоги (должны быть fullscreen на мобилке)

**Приоритет:** ВАЖНЫЙ

---

### 18. Нет обработки состояния "пусто"
**Проблема:**
Некоторые списки могут не иметь красивых пустых состояний.

**Что нужно проверить:**
- Список команды (когда нет членов)
- Список чатов (когда нет сообщений)
- История оплаты (когда нет транзакций)
- Лог AI операций (когда нет операций)

**Пример хорошего Empty State:**
```tsx
<div className="text-center py-12">
  <Icon className="h-12 w-12 text-muted-foreground mx-auto" />
  <h3 className="mt-4 text-lg font-semibold">Пока пусто</h3>
  <p className="text-muted-foreground">Здесь будет отображаться список</p>
  <Button className="mt-4">Добавить</Button>
</div>
```

**Приоритет:** ВАЖНЫЙ

---

### 19. Страницы оплаты не реализованы
**Файлы:**
- `src/pages/hr/payment/success/index.tsx`
- `src/pages/hr/payment/cancel/index.tsx`

**Что нужно проверить:**
Существуют ли эти файлы и что в них? Если заглушки - нужно реализовать:

**Success page:**
- Сообщение об успешной оплате
- Информация о начисленных токенах
- Новый баланс
- Кнопка "Вернуться в дашборд"

**Cancel page:**
- Сообщение об отмене
- Причина (если есть)
- Кнопка "Попробовать снова"
- Кнопка "Вернуться в дашборд"

**Приоритет:** ВАЖНЫЙ

---

### 20. Нет функции сравнения кандидатов в UI
**Проблема:**
Edge Function `compare-candidates` создана, но нет UI для её вызова.

**Где должна быть кнопка:**
1. На странице воронки вакансии
2. В списке кандидатов

**Что нужно:**
1. Добавить чекбоксы для выбора кандидатов (2-5 штук)
2. Кнопка "Сравнить выбранных" (disabled если < 2 или > 5)
3. Диалог с результатами сравнения

**Приоритет:** ВАЖНЫЙ

---

### 21. Чат не тестирован
**Файлы:**
- `src/features/chat/ui/ChatPage.tsx`
- `src/features/chat/ui/ChatArea.tsx`
- И все остальные компоненты чата

**Что нужно:**
1. Протестировать отправку/получение сообщений
2. Проверить realtime обновления
3. Проверить индикатор "печатает..."
4. Проверить отметку "прочитано"
5. Проверить поиск по чатам
6. Проверить скролл к последнему сообщению

**Приоритет:** ВАЖНЫЙ

---

### 22. Воронка не тестирована
**Файл:** `src/pages/hr/vacancy/[id]/funnel/index.tsx`

**Что нужно:**
1. Протестировать Drag & Drop между колонками
2. Проверить обновление статуса в БД
3. Проверить счетчики в каждой колонке
4. Проверить добавление кандидата в воронку
5. Проверить генерацию документов из воронки

**Приоритет:** ВАЖНЫЙ

---

### 23. Рынок талантов - проблемы с фильтрацией
**Файл:** `src/pages/hr/talent-market/index.tsx`

**Что нужно проверить:**
1. Работает ли infinite scroll
2. Корректно ли применяются фильтры
3. Работает ли дебаунс в поиске
4. Корректно ли рассчитывается compatibility score
5. Работает ли покупка кандидата (списание токенов)

**Приоритет:** ВАЖНЫЙ

---

### 24. Нет мониторинга ошибок
**Проблема:**
Отсутствует интеграция с сервисом мониторинга ошибок (Sentry, LogRocket и т.д.).

**Что нужно:**
1. Выбрать сервис (рекомендация: Sentry)
2. Установить и настроить
3. Добавить в Error Boundary
4. Настроить отправку ошибок из Edge Functions

**Приоритет:** ВАЖНЫЙ

---

### 25. Отсутствует CI/CD
**Проблема:**
Нет автоматизированного тестирования и деплоя.

**Что нужно создать:**
```yaml
# .github/workflows/ci.yml
name: CI

on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: actions/setup-node@v3
      - run: npm ci
      - run: npm run lint
      - run: npm run type-check
      - run: npm run build

  deploy:
    needs: test
    if: github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      # Deploy to Vercel
```

**Приоритет:** ВАЖНЫЙ

---

### 26. Billing страница - проверка функционала
**Файл:** `src/pages/hr/billing/index.tsx`

**Что нужно проверить:**
1. Корректно ли загружаются транзакции
2. Корректно ли загружается лог AI операций
3. Работает ли пагинация/сортировка таблиц
4. Отображается ли текущий баланс
5. Есть ли экспорт в CSV/Excel

**Приоритет:** ВАЖНЫЙ

---

### 27. Тесты - проверка ретейк логики
**Проблема:**
Нужно убедиться, что cooldown на пересдачу работает корректно.

**Что проверить:**
1. После прохождения теста `retake_available_at` устанавливается в `now() + 30 days`
2. Кнопка "Пересдать" disabled до этой даты
3. Tooltip показывает когда можно пересдать
4. После истечения срока кнопка становится активной

**Приоритет:** ВАЖНЫЙ

---

### 28. White-label - нет настроек
**Проблема:**
Нет UI для загрузки логотипа и изменения названия организации.

**Что нужно создать:**
1. Страница `/hr/settings`
2. Форма с полями:
   - Название организации
   - Загрузка логотипа (Supabase Storage)
3. API функции:
   - `updateOrganization.ts`
   - `uploadLogo.ts`

**Приоритет:** ВАЖНЫЙ

---

### 29. AI функции используют моки
**Файлы:** Все Edge Functions в `supabase/functions/`

**Что проверить:**
1. Убедиться, что все функции используют реальные вызовы AI
2. Убедиться, что списание токенов работает
3. Убедиться, что логирование работает
4. Проверить обработку ошибок от API (rate limits, etc.)

**Приоритет:** ВАЖНЫЙ

---

### 30. Генерация документов - нет просмотра
**Проблема:**
Документы генерируются, но нет страницы для их просмотра.

**Что нужно создать:**
1. Страница `/public/document/:id`
2. Компонент `DocumentViewer.tsx` с рендерингом markdown
3. Возможность скачать PDF
4. Возможность поделиться ссылкой

**Приоритет:** ВАЖНЫЙ

---

### 31. Email нотификации отсутствуют
**Проблема:**
Нет email уведомлений для важных событий.

**Что нужно реализовать:**
1. Приглашение в команду
2. Приглашение кандидата
3. Завершение теста кандидатом
4. Покупка токенов
5. Новое сообщение в чате

**Технология:** Supabase Edge Functions + SendGrid/Resend

**Приоритет:** ВАЖНЫЙ

---

### 32. Отсутствует документация API
**Проблема:**
Нет документации для разработчиков.

**Что нужно создать:**
1. `docs/API.md` - описание всех API функций
2. `docs/DATABASE.md` - схема БД с описаниями
3. `docs/SETUP.md` - инструкции по развертыванию
4. `docs/EDGE_FUNCTIONS.md` - описание Edge Functions

**Приоритет:** ВАЖНЫЙ

---

## 🟡 МЕЛКИЕ ПРОБЛЕМЫ (Качество кода)

### 33. Console.log по всему коду
**Проблема:**
В 21+ файлах есть console.log/console.error.

**Файлы:**
- `src/features/vacancy-management/ui/VacancyList.tsx`
- `src/features/vacancy-management/ui/IdealProfileGenerator.tsx`
- `src/features/vacancy-management/ui/IdealProfileEditor.tsx`
- `src/features/vacancy-management/api/getVacancyById.ts`
- `src/features/vacancy-management/api/generateIdealProfile.ts`
- `src/features/vacancy-management/api/getApplicationsByVacancy.ts`
- `src/features/testing-system/api/getTests.ts`
- `src/features/testing-system/api/submitTestResults.ts`
- `src/features/testing-system/api/getTestById.ts`
- `src/features/testing-system/api/getTestResultsByCandidate.ts`
- `src/features/talent-market/ui/AcquireCandidateDialog.tsx`
- `src/features/payments/api/getPaymentTransactions.ts`
- `src/features/chat/api/getRecentChatMessages.ts`
- `src/features/chat/api/index.ts`
- `src/features/candidate-management/api/getCandidateProfileByUserId.ts`
- `src/features/candidate-management/api/getActiveApplications.ts`
- `src/features/candidate-management/api/getCandidateProfile.ts`
- `src/features/ai-analysis/ui/GenerateFullAnalysisDialog.tsx`
- `src/features/ai-analysis/api/getAiOperationsLog.ts`
- `src/pages/candidate/test/index.tsx`
- `src/shared/hooks/useHrProfile.ts`

**Также все Edge Functions:**
- `supabase/functions/generate-ideal-profile/index.ts`
- `supabase/functions/analyze-resumes/index.ts`
- `supabase/functions/generate-full-analysis/index.ts`
- `supabase/functions/generate-document/index.ts`
- `supabase/functions/generate-structured-interview/index.ts`
- `supabase/functions/compare-candidates/index.ts`
- `supabase/functions/create-robokassa-invoice/index.ts`
- `supabase/functions/handle-robokassa-result/index.ts`

**Что нужно:**
Удалить все console.log. Для production использовать proper logging service.

**Приоритет:** МЕЛКИЙ

---

### 34. TODO комментарии в коде
**Файлы с TODO:**
- `src/app/router/ProtectedRoute.tsx` (строка 9)
- `src/shared/ui/layouts/DashboardLayout.tsx` (строки 18, 25)
- `src/features/candidate-management/ui/GenerateInviteLinkDialog.tsx` (строки 39-40, 52)
- `src/features/candidate-management/ui/CandidateCard.tsx` (строка 48)
- `src/features/ai-analysis/ui/GenerateFullAnalysisDialog.tsx` (строки 39, 43)
- `src/pages/hr/candidate/profile/index.tsx` (строка 173)
- `src/pages/hr/vacancy/profile/index.tsx` (строки 36, 39)

**Что нужно:**
Завершить все TODO или удалить, если не актуально.

**Приоритет:** МЕЛКИЙ

---

### 35. Использование типа any
**Файл:** `src/pages/hr/talent-market/index.tsx` (строка 86)

**Проблема:**
```tsx
candidates.pages.flat().map((candidate: any) => (
```

**Что нужно:**
```tsx
import type { Database } from '@/shared/types/database'
type Candidate = Database['public']['Tables']['candidates']['Row']

candidates.pages.flat().map((candidate: Candidate) => (
```

**Приоритет:** МЕЛКИЙ

---

### 36. Отсутствуют тесты
**Проблема:**
Нет ни одного тестового файла.

**Что нужно:**
1. Установить testing библиотеки:
   ```bash
   npm install -D vitest @testing-library/react @testing-library/user-event jsdom
   ```
2. Создать тесты для критичных компонентов:
   - Формы (регистрация, создание вакансии)
   - Важные утилиты
   - API функции
3. Настроить CI для запуска тестов

**Приоритет:** МЕЛКИЙ

---

### 37. README.md - дефолтный от Vite
**Файл:** `README.md`

**Проблема:**
Содержит стандартное описание Vite шаблона.

**Что нужно:**
```markdown
# HR Platform v2.0

## Описание
Комплексная платформа для автоматизации подбора персонала с AI-анализом.

## Технологии
- React + TypeScript
- Tailwind CSS + shadcn/ui
- Supabase (Backend)
- Anthropic Claude / Google Gemini (AI)

## Установка
1. `npm install`
2. Скопируйте `.env.example` в `.env.local`
3. Настройте переменные окружения
4. `npm run dev`

## Деплой
См. `docs/SETUP.md`

## Лицензия
...
```

**Приоритет:** МЕЛКИЙ

---

### 38. Loader компонент - TODO
**Файл:** `src/app/router/ProtectedRoute.tsx` (строка 9)

**Проблема:**
```tsx
// TODO: Add a proper loading component
return <div>Loading...</div>
```

**Что нужно:**
Создать красивый брендированный лоадер:
```tsx
// src/shared/ui/Loader.tsx
export const Loader = () => (
  <div className="flex items-center justify-center min-h-screen">
    <div className="animate-spin rounded-full h-12 w-12 border-b-2 border-primary" />
  </div>
)
```

**Приоритет:** МЕЛКИЙ

---

### 39. Hardcoded текст в Team странице
**Файл:** `src/pages/hr/team/index.tsx`

**Проблема:**
Текст на английском, не используются переводы.

**Что нужно:**
```tsx
import { useTranslation } from 'react-i18next'

const { t } = useTranslation('team')
<h1>{t('title')}</h1>
```

И создать файлы:
- `public/locales/ru/team.json`
- `public/locales/en/team.json`
- `public/locales/kk/team.json`

**Приоритет:** МЕЛКИЙ

---

### 40. Отсутствует favicon
**Проблема:**
Нужен кастомный favicon вместо дефолтного Vite.

**Что нужно:**
1. Создать иконку 512x512
2. Сгенерировать все размеры (16, 32, 64, 128, 256, 512)
3. Заменить в `public/`

**Приоритет:** МЕЛКИЙ

---

### 41. Inconsistent кнопки
**Проблема:**
Не везде используются состояния disabled и loading для кнопок.

**Что нужно:**
Проверить все кнопки на:
- Disabled state во время загрузки
- Loading индикатор (spinner)
- Правильные cursor (cursor-not-allowed для disabled)

**Приоритет:** МЕЛКИЙ

---

### 42. Отсутствуют комментарии в коде
**Проблема:**
Сложные функции не имеют JSDoc комментариев.

**Что нужно:**
Добавить JSDoc для:
- Публичных API функций
- Сложных утилит
- RPC функций в базе данных

**Пример:**
```tsx
/**
 * Генерирует идеальный профиль для вакансии с помощью AI
 * @param vacancyId - ID вакансии
 * @param hrSpecialistId - ID HR специалиста
 * @returns Promise с результатом генерации
 */
export async function generateIdealProfile(...)
```

**Приоритет:** МЕЛКИЙ

---

### 43. Нет pre-commit hooks
**Проблема:**
Нет автоматической проверки кода перед коммитом.

**Что нужно:**
```bash
npm install -D husky lint-staged

# .husky/pre-commit
npm run lint
npm run type-check

# package.json
"lint-staged": {
  "*.{ts,tsx}": ["eslint --fix", "prettier --write"]
}
```

**Приоритет:** МЕЛКИЙ

---

### 44. Отсутствует .gitignore для IDE
**Проблема:**
В .gitignore нет исключений для IDE файлов.

**Что добавить:**
```
# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store
```

**Приоритет:** МЕЛКИЙ

---

### 45. Нет sitemap.xml
**Проблема:**
Для SEO нужен sitemap (если будут публичные страницы).

**Что нужно:**
Если будут публичные страницы (просмотр вакансий, результаты тестов) - создать sitemap.xml.

**Приоритет:** МЕЛКИЙ

---

### 46. Отсутствуют meta теги
**Проблема:**
Нет Open Graph и Twitter Card мета-тегов для шаринга.

**Что нужно в index.html:**
```html
<meta property="og:title" content="HR Platform v2.0" />
<meta property="og:description" content="..." />
<meta property="og:image" content="..." />
<meta name="twitter:card" content="summary_large_image" />
```

**Приоритет:** МЕЛКИЙ

---

### 47. Нет обработки offline режима
**Проблема:**
Нет индикации потери соединения.

**Что нужно:**
```tsx
// src/shared/hooks/useOnline.ts
export const useOnline = () => {
  const [isOnline, setIsOnline] = useState(navigator.onLine)

  useEffect(() => {
    const handleOnline = () => setIsOnline(true)
    const handleOffline = () => setIsOnline(false)

    window.addEventListener('online', handleOnline)
    window.addEventListener('offline', handleOffline)

    return () => {
      window.removeEventListener('online', handleOnline)
      window.removeEventListener('offline', handleOffline)
    }
  }, [])

  return isOnline
}
```

**Приоритет:** МЕЛКИЙ

---

### 48. Отсутствует analytics
**Проблема:**
Нет отслеживания пользовательского поведения.

**Что нужно:**
Интегрировать Google Analytics или Plausible Analytics.

**Приоритет:** МЕЛКИЙ

---

### 49. Нет rate limiting на клиенте
**Проблема:**
Можно спамить запросами к AI функциям.

**Что нужно:**
Добавить debounce/throttle для дорогих операций:
```tsx
import { useMutation } from '@tanstack/react-query'
import { useDebounce } from '@/shared/hooks/useDebounce'

// Или использовать react-query retry и staleTime
```

**Приоритет:** МЕЛКИЙ

---

### 50. Документация миграций отсутствует
**Проблема:**
Нет описания порядка применения миграций и их зависимостей.

**Что нужно:**
Создать `supabase/migrations/README.md` с:
- Порядком применения
- Описанием каждой миграции
- Rollback инструкциями (если нужно)

**Приоритет:** МЕЛКИЙ

---

## 📊 ИТОГОВАЯ СТАТИСТИКА

### По приоритетам:
- 🔴 **Критические:** 8 проблем
- 🟠 **Важные:** 24 проблемы
- 🟡 **Мелкие:** 18 проблем

**ВСЕГО:** 50 проблем

### По категориям:
- **Отсутствующий функционал:** 15
- **Нерабочие кнопки/функции:** 8
- **Качество кода:** 12
- **Безопасность:** 2
- **Документация:** 4
- **DevOps/CI/CD:** 3
- **UX/Дизайн:** 4
- **Performance:** 2

---

## 🎯 РЕКОМЕНДУЕМЫЙ ПЛАН ДОРАБОТКИ

### Фаза 1: Критические проблемы (2 недели)
**Цель:** Сделать приложение рабочим

1. Исправить типы БД (проблема #4)
2. Создать .env.example (проблема #5)
3. Реализовать полноценный Header с навигацией (проблема #1)
4. Создать страницы ошибок (проблема #3)
5. Реализовать Team Management (проблема #2)
6. Создать регистрацию по токену (проблема #6)
7. Реализовать редактирование профиля кандидата (проблема #8)
8. Провести аудит RLS политик (проблема #7)

**Результат:** Все критические функции работают

---

### Фаза 2: Важные улучшения (3 недели)
**Цель:** Улучшить UX и завершить функции

**Неделя 1:**
1. Реализовать все нерабочие кнопки (#9, #10, #11)
2. Заполнить справочники (#13)
3. Добавить toast уведомления (#14)
4. Исправить отображение категорий и email (#12, #10)

**Неделя 2:**
5. Реализовать страницы оплаты (#19)
6. Добавить White-label настройки (#28)
7. Протестировать чат (#21)
8. Протестировать воронку (#22)
9. Протестировать рынок талантов (#23)

**Неделя 3:**
10. Мобильная адаптация (#17)
11. Валидация форм (#16)
12. Генерация PDF (#10, #11)
13. Email нотификации (#31)
14. Мониторинг ошибок (#24)
15. CI/CD (#25)

**Результат:** Приложение production-ready

---

### Фаза 3: Полировка (1-2 недели)
**Цель:** Код качество и документация

1. Удалить все console.log (#33)
2. Завершить все TODO (#34)
3. Исправить типы any (#35)
4. Добавить тесты (#36)
5. Обновить README (#37)
6. Добавить документацию API (#32, #50)
7. Добавить pre-commit hooks (#43)
8. Добавить analytics (#48)
9. Остальные мелкие улучшения

**Результат:** Код чистый, задокументированный, поддерживаемый

---

## ⏱️ ОБЩЕЕ ВРЕМЯ

- **Минимум (только критические + важные):** 5 недель
- **Оптимально (все фазы):** 6-7 недель
- **С запасом (тестирование, баги):** 8-10 недель

---

## 💰 ОЦЕНКА ТРУДОЗАТРАТ

Если 1 разработчик full-time:
- **Фаза 1:** 80 часов (2 недели)
- **Фаза 2:** 120 часов (3 недели)
- **Фаза 3:** 60 часов (1.5 недели)

**ИТОГО:** ~260 часов работы

---

## 📝 ЗАМЕТКИ

1. **Приоритизация:** Начинать с критических проблем
2. **Тестирование:** После каждой фазы полное тестирование
3. **Code review:** Желательно второй разработчик для ревью
4. **Документация:** Обновлять параллельно с разработкой
5. **Безопасность:** Аудит RLS - критически важен перед продакшном

---

**Дата составления:** 17 ноября 2025
**Версия документа:** 1.0
**Автор:** Claude (AI Assistant)
