# Google OAuth Integration - Status & Next Steps
**HR Platform v2.0**  
**Дата:** 13 декабря 2024  
**Supabase Project ID:** `enbtguqnsoraxddgddah`  
**Google Cloud Project:** `circular-jet-459908-s3`

---

## ✅ ЧТО УЖЕ СДЕЛАНО

### 1. Google Cloud Console - Полностью настроен

#### OAuth Consent Screen создан:
- **App name:** HR Platform v2.0
- **User support email:** Настроен
- **User Type:** External
- **Publishing Status:** ✅ In Production (опубликовано)
- **Scopes:** Non-sensitive (email, profile) - автоматически включены

#### OAuth Client ID создан:
- **Application Type:** Web application
- **Client ID:** `[REDACTED]`
- **Client Secret:** `[REDACTED]`

#### Authorized JavaScript origins:
- ✅ `https://hr.labpro.in`
- ✅ `https://enbtguqnsoraxddgddah.supabase.co`

#### Authorized redirect URIs:
- ✅ `https://enbtguqnsoraxddgddah.supabase.co/auth/v1/callback`

### 2. Текущий статус приложения

**Статус верификации:**
- ❌ **Unverified** (не верифицировано)
- ⚠️ Показывается предупреждение "This app hasn't been verified by Google yet"
- ⚠️ **Лимит: 100 пользователей** на весь срок жизни проекта
- ✅ Приложение работает, но пользователи должны нажать "Advanced → Continue"

**Verification Center показывает:**
```
Verification Status: Verification not required
```
Это означает что для базовых scopes (email/profile) полная верификация НЕ обязательна, но **Brand Verification нужна** для снятия предупреждения и лимита.

---

## 🔧 ЧТО НУЖНО СДЕЛАТЬ

### ЧАСТЬ 1: Brand Verification (убрать "Unverified app" и лимит 100 users)

Это **обязательно** для production запуска без ограничений.

#### Шаг 1.1: Создать Privacy Policy (на 3 языках)

**Требования:**
- Должен быть публично доступен на домене `hr.labpro.in`
- Должен описывать использование данных Google OAuth
- Рекомендуемый URL: `https://hr.labpro.in/privacy` или `/privacy-policy`

**Языковые версии:**
- Русский: `/privacy?lang=ru`
- English: `/privacy?lang=en`
- Қазақша: `/privacy?lang=kk`

**Обязательные пункты в Privacy Policy:**
1. **Какие данные собираем через Google OAuth:**
   - Email address
   - Full name
   - Profile picture URL
   
2. **Как используем данные:**
   - Для аутентификации пользователя
   - Для отображения профиля в системе
   - Для коммуникации с пользователем
   
3. **Где храним данные:**
   - Supabase (PostgreSQL database)
   - Encrypted storage
   
4. **Как долго храним:**
   - До удаления аккаунта пользователем
   
5. **Права пользователя:**
   - Право на доступ к своим данным
   - Право на удаление аккаунта
   - Право на экспорт данных
   
6. **Безопасность:**
   - Row Level Security (RLS)
   - Encrypted connections (HTTPS)
   
7. **Контактная информация:**
   - Support email для вопросов о privacy

**Минимальный шаблон (см. ниже в секции "Примеры").**

#### Шаг 1.2: Обновить Homepage

**Требования:**
- Главная страница `https://hr.labpro.in` должна содержать:
  - Описание HR Platform (что делает приложение)
  - Ссылку на Privacy Policy (обязательно в footer!)
  - Контактную информацию (email для support)
  
**Если главная уже есть:**
- Просто добавить ссылку на Privacy Policy in footer
- Убедиться что есть краткое описание платформы

#### Шаг 1.3: Верифицировать домен в Google Search Console

**Процесс:**
1. Открыть: https://search.google.com/search-console
2. Использовать **тот же Google аккаунт**, что и для Google Cloud Console
3. Нажать "Add Property"
4. Выбрать "Domain" (не URL prefix!)
5. Ввести: `hr.labpro.in`

**Методы верификации:**

**Вариант A - DNS (рекомендуется):**
```
1. Google даст TXT запись типа:
   google-site-verification=abc123xyz456...
   
2. Добавить в DNS настройки домена hr.labpro.in:
   Type: TXT
   Host: @ или hr.labpro.in
   Value: google-site-verification=abc123xyz456...
   
3. Подождать 5-10 минут
4. Вернуться в Search Console и нажать "Verify"
```

**Вариант B - HTML файл:**
```
1. Скачать файл google[xxxxx].html от Google
2. Загрузить в корень сайта: hr.labpro.in/google[xxxxx].html
3. Убедиться что файл доступен публично
4. Нажать "Verify"
```

**КРИТИЧЕСКИ ВАЖНО:** Используйте один и тот же Google аккаунт для Search Console и Google Cloud Console!

#### Шаг 1.4: Заполнить Branding в Google Cloud Console

**Путь:** Google Auth Platform → Branding

**Поля для заполнения:**

| Поле | Значение | Обязательно |
|------|----------|-------------|
| App name | `HR Platform v2.0` | ✅ Да |
| App logo | PNG/JPG, минимум 120x120px | ⚠️ Рекомендуется |
| Support email | Рабочий email | ✅ Да |
| App homepage | `https://hr.labpro.in` | ✅ Да |
| Privacy Policy link | `https://hr.labpro.in/privacy` | ✅ Да |
| Terms of Service link | `https://hr.labpro.in/terms` | ⚠️ Опционально |
| Authorized domains | `hr.labpro.in` | ✅ Да |

**Примечание:** После заполнения нажать **Save**.

#### Шаг 1.5: Подать на Brand Verification

**Процесс:**
1. После заполнения всех полей в Branding
2. Перейти: Google Auth Platform → Verification Center
3. Должна появиться кнопка **"Submit for verification"**
4. Нажать и подтвердить
5. Google отправит email на Support email с подтверждением

**Ожидаемое время:** 2-3 рабочих дня

**После одобрения:**
- ✅ Исчезнет "Unverified app" предупреждение
- ✅ Снимется лимит 100 пользователей
- ✅ Пользователи увидят красивый OAuth экран с вашим логотипом

---

### ЧАСТЬ 2: Supabase Configuration

#### Шаг 2.1: Включить Google Provider в Supabase

**Путь:** Supabase Dashboard → Authentication → Providers → Google

**Настройки:**
```
☑ Enable Sign in with Google

Client ID (for OAuth):
[REDACTED]

Client Secret (for OAuth):
[REDACTED]

Skip nonce check:
☐ Оставить выключенным (для безопасности)
```

Нажать **Save**.

#### Шаг 2.2: Настроить Redirect URLs в Supabase

**Путь:** Supabase Dashboard → Authentication → URL Configuration

**Site URL:**
```
https://hr.labpro.in
```

**Redirect URLs (добавить все):**
```
https://hr.labpro.in/**
https://hr.labpro.in/auth/callback
http://localhost:5173/**
http://localhost:5173/auth/callback
```

Нажать **Save**.

#### Шаг 2.3: Проверить RLS политики (если еще не настроены)

**Таблица `profiles`** должна поддерживать создание профилей при OAuth:

```sql
-- Политика для создания профиля при первом входе через OAuth
CREATE POLICY "Users can create their own profile"
ON profiles FOR INSERT
WITH CHECK (auth.uid() = id);

-- Политика для чтения своего профиля
CREATE POLICY "Users can read their own profile"
ON profiles FOR SELECT
USING (auth.uid() = id);

-- Политика для обновления своего профиля
CREATE POLICY "Users can update their own profile"
ON profiles FOR UPDATE
USING (auth.uid() = id);
```

#### Шаг 2.4: Создать триггер для автоматического создания профиля (рекомендуется)

```sql
-- Функция для автоматического создания профиля при регистрации
CREATE OR REPLACE FUNCTION public.handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO public.profiles (id, email, full_name, avatar_url, role)
  VALUES (
    NEW.id,
    NEW.email,
    COALESCE(NEW.raw_user_meta_data->>'full_name', NEW.raw_user_meta_data->>'name', ''),
    COALESCE(NEW.raw_user_meta_data->>'avatar_url', NEW.raw_user_meta_data->>'picture', ''),
    'candidate'  -- По умолчанию все новые пользователи - кандидаты
  );
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

-- Триггер на создание нового пользователя
CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW
  EXECUTE FUNCTION public.handle_new_user();
```

**Примечание:** Если триггер уже существует для email/password регистрации, он будет работать и для Google OAuth.

---

### ЧАСТЬ 3: Frontend Implementation

#### Шаг 3.1: Обновить страницу Login

**Файл:** `src/pages/Login.tsx` (или где у вас форма логина)

**Добавить:**
1. Кнопку "Войти через Google"
2. Функцию `handleGoogleLogin`
3. Разделитель между email/password и OAuth
4. Google иконку (SVG)

**Основная логика:**
```typescript
const handleGoogleLogin = async () => {
  try {
    setLoading(true);
    
    const { data, error } = await supabase.auth.signInWithOAuth({
      provider: 'google',
      options: {
        redirectTo: `${window.location.origin}/auth/callback`,
        queryParams: {
          access_type: 'offline',
          prompt: 'consent',
        },
      },
    });

    if (error) throw error;
    // OAuth автоматически перенаправит пользователя на Google
  } catch (error: any) {
    console.error('Google login error:', error);
    toast.error(t('auth.googleLoginError'));
  } finally {
    setLoading(false);
  }
};
```

**UI компоненты:**
- Button с вариантом "outline"
- Google SVG иконка
- Текст с i18n переводом
- Loading state

#### Шаг 3.2: Создать страницу AuthCallback

**Файл:** `src/pages/AuthCallback.tsx` (новый файл)

**Задачи:**
1. Получить session из URL
2. Проверить существование профиля
3. Создать профиль если его нет (если триггер не настроен)
4. Перенаправить на dashboard в зависимости от роли
5. Показать loading индикатор

**Основная логика:**
```typescript
useEffect(() => {
  const handleCallback = async () => {
    try {
      // Получаем сессию
      const { data: { session }, error } = await supabase.auth.getSession();
      
      if (error) throw error;
      
      if (session) {
        // Проверяем профиль
        const { data: profile, error: profileError } = await supabase
          .from('profiles')
          .select('*')
          .eq('id', session.user.id)
          .single();

        // Если профиля нет - создаем
        if (profileError && profileError.code === 'PGRST116') {
          const userData = session.user.user_metadata;
          
          await supabase.from('profiles').insert({
            id: session.user.id,
            email: session.user.email!,
            full_name: userData.full_name || userData.name || '',
            avatar_url: userData.avatar_url || userData.picture || '',
            role: 'candidate',
          });
        }

        // Обновляем store
        setUser(session.user);

        // Перенаправляем
        if (profile?.role === 'hr') {
          navigate('/hr/dashboard');
        } else {
          navigate('/candidate/dashboard');
        }

        toast.success(t('auth.loginSuccess'));
      } else {
        navigate('/login');
      }
    } catch (error) {
      console.error('Auth callback error:', error);
      toast.error(t('auth.authError'));
      navigate('/login');
    }
  };

  handleCallback();
}, []);
```

#### Шаг 3.3: Добавить маршрут для callback

**Файл:** `src/App.tsx` или `src/router.tsx`

```typescript
import AuthCallback from '@/pages/AuthCallback';

// В массиве маршрутов:
{
  path: '/auth/callback',
  element: <AuthCallback />,
}
```

#### Шаг 3.4: Добавить переводы (i18n)

**Файлы:**
- `src/i18n/locales/ru.json`
- `src/i18n/locales/en.json`
- `src/i18n/locales/kk.json`

**Добавить ключи:**
```json
{
  "auth": {
    "signInWithGoogle": "Войти через Google / Sign in with Google / Google арқылы кіру",
    "orContinueWith": "Или продолжить с / Or continue with / Немесе жалғастыру",
    "googleLoginError": "Ошибка входа через Google / Google login error / Google арқылы кіру қатесі",
    "loginSuccess": "Вход выполнен успешно / Login successful / Кіру сәтті өтті",
    "authError": "Ошибка авторизации / Authentication error / Авторизация қатесі"
  }
}
```

#### Шаг 3.5: Добавить Google OAuth на страницу Registration (опционально)

Если хотите, чтобы пользователи могли регистрироваться через Google:
- Повторить те же шаги что и для Login
- Добавить кнопку "Зарегистрироваться через Google"
- Использовать ту же функцию `handleGoogleLogin`

---

## 📄 ПРИМЕРЫ

### Пример минимального Privacy Policy (Русский)

```markdown
# Политика конфиденциальности HR Platform v2.0

**Дата последнего обновления:** 13 декабря 2024

## 1. Сбор информации

### 1.1 Информация через Google OAuth
При входе через Google мы собираем:
- Адрес электронной почты
- Полное имя
- Фотографию профиля

### 1.2 Цель сбора
Эти данные используются для:
- Аутентификации пользователя в системе
- Отображения профиля в платформе
- Коммуникации с пользователем

## 2. Хранение данных

### 2.1 Где хранятся данные
Все данные безопасно хранятся в базе данных Supabase (PostgreSQL) с использованием:
- Шифрования при передаче (HTTPS/TLS)
- Row Level Security (RLS) для разграничения доступа
- Регулярного резервного копирования

### 2.2 Срок хранения
Данные хранятся до момента удаления аккаунта пользователем.

## 3. Использование данных

Мы НЕ:
- Не продаем ваши данные третьим лицам
- Не используем данные для рекламы
- Не передаем данные без вашего согласия

Мы используем данные только для:
- Предоставления функционала HR платформы
- Улучшения сервиса
- Технической поддержки

## 4. Права пользователя

Вы имеете право:
- Получить доступ к своим данным
- Исправить неточные данные
- Удалить свой аккаунт и все связанные данные
- Экспортировать свои данные

## 5. Безопасность

Мы применяем современные меры безопасности:
- Шифрование данных
- Защита от несанкционированного доступа
- Регулярные аудиты безопасности

## 6. Контакты

По вопросам конфиденциальности:
- Email: [ваш support email]
- Адрес: [опционально]

## 7. Изменения политики

Мы можем обновлять эту политику. О значительных изменениях мы уведомим пользователей по email.
```

### Пример Privacy Policy (English)

```markdown
# Privacy Policy - HR Platform v2.0

**Last Updated:** December 13, 2024

## 1. Information Collection

### 1.1 Google OAuth Information
When you sign in with Google, we collect:
- Email address
- Full name
- Profile picture

### 1.2 Purpose
This information is used to:
- Authenticate you in the system
- Display your profile in the platform
- Communicate with you

## 2. Data Storage

### 2.1 Where We Store Data
All data is securely stored in Supabase database (PostgreSQL) with:
- Encryption in transit (HTTPS/TLS)
- Row Level Security (RLS)
- Regular backups

### 2.2 Retention Period
Data is stored until account deletion by the user.

## 3. Data Usage

We DO NOT:
- Sell your data to third parties
- Use data for advertising
- Share data without your consent

We use data only for:
- Providing HR platform functionality
- Service improvement
- Technical support

## 4. Your Rights

You have the right to:
- Access your data
- Correct inaccurate data
- Delete your account and all associated data
- Export your data

## 5. Security

We implement modern security measures:
- Data encryption
- Protection against unauthorized access
- Regular security audits

## 6. Contact

For privacy inquiries:
- Email: [your support email]
- Address: [optional]

## 7. Policy Changes

We may update this policy. Significant changes will be notified via email.
```

### Пример Privacy Policy (Қазақша)

```markdown
# Құпиялылық саясаты - HR Platform v2.0

**Соңғы жаңарту:** 13 желтоқсан 2024

## 1. Ақпарат жинау

### 1.1 Google OAuth арқылы ақпарат
Google арқылы кірген кезде біз жинаймыз:
- Электрондық пошта мекенжайы
- Толық аты-жөні
- Профиль суреті

### 1.2 Мақсаты
Бұл деректер үшін қолданылады:
- Жүйеде аутентификация
- Платформада профильді көрсету
- Пайдаланушымен байланыс

## 2. Деректерді сақтау

### 2.1 Деректер қайда сақталады
Барлық деректер Supabase дерекқорында (PostgreSQL) қауіпсіз сақталады:
- Тасымалдау кезінде шифрлау (HTTPS/TLS)
- Жол деңгейінде қауіпсіздік (RLS)
- Тұрақты сақтық көшірмелер

### 2.2 Сақтау мерзімі
Деректер пайдаланушы тіркелгісін жойғанға дейін сақталады.

## 3. Деректерді пайдалану

Біз ЖАСАМАЙМЫЗ:
- Деректеріңізді үшінші тараптарға сатамыз
- Деректерді жарнама үшін пайдаланамыз
- Келісіміңізсіз деректерді бермейміз

Біз деректерді тек үшін пайдаланамыз:
- HR платформасының функционалдығын қамтамасыз ету
- Қызметті жақсарту
- Техникалық қолдау

## 4. Пайдаланушы құқықтары

Сізде құқық бар:
- Деректеріңізге қол жеткізу
- Дұрыс емес деректерді түзету
- Тіркелгіңізді және барлық байланысты деректерді жою
- Деректеріңізді экспорттау

## 5. Қауіпсіздік

Біз заманауи қауіпсіздік шараларын қолданамыз:
- Деректерді шифрлау
- Рұқсатсыз қол жеткізуден қорғау
- Тұрақты қауіпсіздік аудиттері

## 6. Байланыс

Құпиялылық сұрақтары бойынша:
- Email: [сіздің support email]
- Мекенжай: [қосымша]

## 7. Саясаттағы өзгерістер

Біз осы саясатты жаңарта аламыз. Маңызды өзгерістер туралы email арқылы хабарлаймыз.
```

---

## 🎯 ПРИОРИТЕТЫ

### КРИТИЧЕСКИ ВАЖНО (без этого OAuth не будет работать):
1. ✅ **Supabase настройка** (Шаг 2.1-2.2)
2. ✅ **Frontend код** (Шаг 3.1-3.3)
3. ✅ **Маршруты** (Шаг 3.3)

### ВАЖНО ДЛЯ PRODUCTION (убрать Unverified и лимит):
4. 📝 **Privacy Policy** (Шаг 1.1)
5. 🏠 **Homepage обновление** (Шаг 1.2)
6. 🔐 **Domain verification** (Шаг 1.3)
7. 🎨 **Branding** (Шаг 1.4)
8. ✉️ **Submit for verification** (Шаг 1.5)

### РЕКОМЕНДУЕТСЯ:
9. 🗄️ **Триггер для автопрофиля** (Шаг 2.4)
10. 🌐 **Переводы i18n** (Шаг 3.4)
11. 📋 **Registration страница** (Шаг 3.5)

---

## 📊 ТЕКУЩИЙ СТАТУС

```
┌─────────────────────────────────────────┐
│ Google Cloud Console                    │
│ ✅ OAuth Client создан                  │
│ ✅ Опубликовано (In Production)         │
│ ⚠️  Unverified (показывается warning)   │
│ ⚠️  Лимит: 100 пользователей            │
└─────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│ Supabase                                │
│ ⏳ Google Provider - нужно настроить    │
│ ⏳ Redirect URLs - нужно настроить      │
└─────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│ Frontend                                │
│ ⏳ Login page - добавить кнопку Google  │
│ ⏳ AuthCallback - создать страницу      │
│ ⏳ Router - добавить маршрут            │
│ ⏳ i18n - добавить переводы             │
└─────────────────────────────────────────┘
           │
           ▼
┌─────────────────────────────────────────┐
│ Brand Verification (для production)     │
│ ⏳ Privacy Policy - создать             │
│ ⏳ Domain verification - настроить      │
│ ⏳ Branding - заполнить                 │
│ ⏳ Submit - подать заявку               │
└─────────────────────────────────────────┘
```

---

## 🔗 ПОЛЕЗНЫЕ ССЫЛКИ

### Google Cloud Console:
- Проект: https://console.cloud.google.com/apis/credentials?project=circular-jet-459908-s3
- OAuth Consent: https://console.cloud.google.com/apis/credentials/consent?project=circular-jet-459908-s3
- Google Auth Platform: https://console.cloud.google.com/auth/overview?project=circular-jet-459908-s3

### Supabase:
- Dashboard: https://supabase.com/dashboard/project/enbtguqnsoraxddgddah
- Auth Settings: https://supabase.com/dashboard/project/enbtguqnsoraxddgddah/auth/providers

### Google Search Console:
- Добавить домен: https://search.google.com/search-console

### Документация:
- Supabase Auth Google: https://supabase.com/docs/guides/auth/social-login/auth-google
- Google OAuth Verification: https://developers.google.com/identity/protocols/oauth2/production-readiness/brand-verification

---

## ⚙️ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Environment Variables (если нужны):
```env
# Не нужны для фронтенда - credentials хранятся в Supabase Dashboard
# Supabase автоматически управляет OAuth flow
```

### Supabase Auth URL (автоматический):
```
https://enbtguqnsoraxddgddah.supabase.co/auth/v1/callback
```

### Callback flow:
```
User clicks "Sign in with Google"
   ↓
Redirect to Google OAuth consent screen
   ↓
User approves
   ↓
Google redirects to: https://enbtguqnsoraxddgddah.supabase.co/auth/v1/callback
   ↓
Supabase processes token
   ↓
Supabase redirects to: https://hr.labpro.in/auth/callback#access_token=...
   ↓
Frontend AuthCallback page handles session
   ↓
Create profile if needed
   ↓
Redirect to dashboard
```

### User Metadata из Google:
```typescript
session.user.user_metadata = {
  avatar_url: "https://lh3.googleusercontent.com/...",
  email: "user@gmail.com",
  email_verified: true,
  full_name: "John Doe",
  iss: "https://accounts.google.com",
  name: "John Doe",
  picture: "https://lh3.googleusercontent.com/...",
  provider_id: "1234567890",
  sub: "1234567890"
}
```

### RLS Context:
```sql
-- После Google OAuth в базе будет:
auth.uid() = user_id из Google OAuth
auth.email() = email из Google аккаунта
```

---

## ❓ FAQ

**Q: Можно ли тестировать OAuth без верификации?**  
A: Да! OAuth работает сразу, но с предупреждением "Unverified app" и лимитом 100 пользователей.

**Q: Сколько ждать Brand Verification?**  
A: 2-3 рабочих дня после подачи заявки.

**Q: Нужно ли видео для Brand Verification?**  
A: НЕТ! Видео нужно только для sensitive/restricted scopes (Gmail, Drive, Calendar). Для простого логина (email/profile) видео НЕ требуется.

**Q: Что если у меня нет Privacy Policy?**  
A: Используйте шаблоны из этого документа и адаптируйте под свой проект.

**Q: Можно ли использовать один Google Project для dev и prod?**  
A: Можно, но Google рекомендует разные проекты. Для начала можно использовать один.

**Q: Что делать если пользователь уже зарегистрирован через email, а потом входит через Google?**  
A: Supabase автоматически свяжет аккаунты если email совпадает (Identity Linking).

**Q: Как удалить Google OAuth привязку?**  
A: Через `supabase.auth.unlinkIdentity({ identity_id: 'xxx' })`.

---

## 📝 ЧЕКЛИСТ ДЛЯ РАЗРАБОТКИ

### MVP (минимум для работы):
- [ ] Настроить Google Provider в Supabase
- [ ] Настроить Redirect URLs в Supabase
- [ ] Добавить кнопку "Войти через Google" на Login
- [ ] Создать страницу AuthCallback
- [ ] Добавить маршрут /auth/callback
- [ ] Протестировать локально

### Production Ready (убрать ограничения):
- [ ] Создать Privacy Policy на 3 языках
- [ ] Разместить на hr.labpro.in/privacy
- [ ] Добавить ссылку на Privacy Policy in footer
- [ ] Верифицировать домен в Google Search Console
- [ ] Заполнить Branding в Google Cloud Console
- [ ] Подать на Brand Verification
- [ ] Дождаться одобрения (2-3 дня)

### Nice to Have:
- [ ] Добавить триггер автосоздания профиля
- [ ] Добавить переводы для всех OAuth сообщений
- [ ] Добавить Google OAuth на Registration
- [ ] Добавить Terms of Service
- [ ] Добавить логотип в Branding

---

**Готово к разработке!** 🚀  
Этот документ содержит всю необходимую информацию для завершения интеграции Google OAuth.
