# Сравнение Flow: Текущая vs Умная воронка

## 🔴 ТЕКУЩИЙ FLOW (Проблемный)

```
HR добавляет кандидата
         ↓
    [INVITED] ← Застрял здесь!
         ↓ (кандидат проходит тесты)
    [INVITED] ← Статус не меняется
         ↓ (HR вручную перемещает)
    [TESTING]
         ↓ (кандидат завершает 6/6)
    [TESTING] ← Статус не меняется
         ↓ (HR вручную перемещает)
   [EVALUATED]
         ↓
    HR переходит на страницу профиля
         ↓
    Кликает "Полный анализ" (AI)
         ↓
    Ждет генерации
         ↓
    Кликает "Сгенерировать интервью"
         ↓
    Ждет генерации
         ↓
    Возвращается в воронку
         ↓
    Вручную перемещает в [INTERVIEW]
         ↓
    Снова на страницу профиля
         ↓
    Кликает "Провести интервью"
         ↓
    Заполняет, завершает (hire_strongly)
         ↓
    Возвращается в воронку
         ↓
    Вручную перемещает в [OFFER]
         ↓
    Снова на страницу профиля
         ↓
    Кликает "Сгенерировать оффер"
         ↓
    Возвращается в воронку
         ↓
    Вручную перемещает в [HIRED]

ИТОГО: ~15-20 кликов + много переходов между страницами
```

---

## 🟢 НОВЫЙ FLOW (Умная воронка)

```
HR добавляет кандидата
         ↓
    [INVITED]
         ↓ ⚡ АВТОМАТИЧЕСКИ (кандидат начал первый тест)
    [TESTING]
    Badge: "3/6 тестов" (обновляется в реальном времени)
         ↓ ⚡ АВТОМАТИЧЕСКИ (завершил 6/6)
   [EVALUATED]
    Badge: "✅ Все тесты пройдены"
    Toast: "Иван готов к анализу!"
         ↓
    HR кликает кнопку "📊 Полный анализ" (прямо в карточке)
         ↓
    Overlay с результатом (без ухода со страницы)
    Badge: "Анализ готов"
         ↓
    HR кликает "📅 Пригласить на интервью" (в карточке)
         ↓ ⚡ АВТОМАТИЧЕСКИ переход в [INTERVIEW]
    Toast: "План интервью готов"
    Badge: "Интервью запланировано"
         ↓
    HR кликает "📝 Провести интервью" (в карточке)
         ↓
    InterviewWorkspace открывается
         ↓
    HR заполняет и завершает (recommendation: hire_strongly)
         ↓ ⚡ АВТОМАТИЧЕСКИ (если reject → rejected)
    Карточка обновляется:
    Badge: "🌟 Нанять немедленно!" (пульсирует)
    Кнопка: "💼 Отправить оффер" (яркая, emerald)
         ↓
    HR кликает "Отправить оффер" (в карточке)
         ↓ ⚡ АВТОМАТИЧЕСКИ переход в [OFFER]
    Оффер генерируется в фоне
         ↓
    (через несколько дней)
    HR кликает "Принял оффер" (в карточке)
         ↓
    [HIRED] 🎉 Конфетти!

ИТОГО: 5-6 кликов + все в одном месте (воронка)
```

---

## 📊 СРАВНИТЕЛЬНАЯ ТАБЛИЦА

| Аспект | Текущее | Новое |
|--------|---------|-------|
| **Автопереходов** | 0 | 3 (invited→testing, testing→evaluated, reject) |
| **Кликов для найма** | 15-20 | 5-6 |
| **Переходов между страницами** | 5-7 | 0-1 |
| **AI прямо в воронке** | Нет | Да (анализ, интервью, оффер) |
| **Видимость прогресса** | Нет | Да (badges в реальном времени) |
| **Контекстные подсказки** | Нет | Да (система предлагает шаги) |
| **Timeline событий** | Нет | Да (полная история) |

---

## 🎯 КЛЮЧЕВЫЕ УЛУЧШЕНИЯ

### 1. Воронка = Единая точка управления
**Было:** Воронка → Профиль → Генерация → Воронка  
**Стало:** Все в воронке, профиль только для истории

### 2. Умные badges (живые)
**Было:** Статичная цифра "3/6 тестов"  
**Стало:** 
- "Начал тестирование" (синий) при 1-2 тестах
- "Проходит тесты" (фиолетовый) при 3-4
- "Почти завершил" (оранжевый, пульсация) при 5/6
- "Готов к анализу" (зеленый) при 6/6

### 3. Контекстные действия
**Было:** Универсальные кнопки "Профиль, Чат, Переместить"  
**Стало:** Кнопки зависят от статуса:
- `evaluated` → "Полный анализ", "Пригласить на интервью"
- `interview (hire)` → "Отправить оффер" (пульсирует!)
- `offer` → "Принял", "Отказался"

### 4. Интеграция интервью с воронкой
**Было:** Интервью отдельно, результат не влияет на воронку  
**Стало:** 
- Завершение интервью → автоматом обновляет карточку
- Рекомендация `reject` → автоматом в `rejected`
- Рекомендация `hire*` → показывает кнопку оффера

### 5. Страница профиля = читалка
**Было:** Кнопки генерации (дублирование с воронкой)  
**Стало:** Только история + табы для удобства

---

## 💡 ПРИМЕРЫ UI (Псевдокод)

### Карточка с прогрессом тестирования

```tsx
{status === 'testing' && (
  <>
    <div className="flex items-center justify-between mb-2">
      <span className="text-xs text-muted-foreground">Прогресс тестирования</span>
      <Badge variant={getTestProgressVariant(tests_completed)}>
        {tests_completed}/6
      </Badge>
    </div>
    <Progress value={(tests_completed / 6) * 100} className="h-2" />
    {tests_completed === 5 && (
      <p className="text-xs text-amber-600 mt-1 animate-pulse">
        Один тест до завершения!
      </p>
    )}
  </>
)}
```

### Умная кнопка оффера

```tsx
{status === 'interview' && interview_recommendation === 'hire_strongly' && (
  <Button
    className="w-full bg-gradient-to-r from-emerald-600 to-teal-600 hover:from-emerald-700 hover:to-teal-700 animate-pulse shadow-lg shadow-emerald-500/50"
    onClick={handleSendOffer}
  >
    <Sparkles className="h-4 w-4 mr-2 animate-spin" />
    Отправить оффер
    <ArrowRight className="h-4 w-4 ml-2" />
  </Button>
)}
```

### Toast с автопереходом

```tsx
// При завершении 6 тестов
toast.success("Иван Петров завершил все тесты!", {
  description: "Кандидат автоматически перемещен в 'Оценён'",
  action: {
    label: "Провести анализ",
    onClick: () => handleGenerateAnalysis(candidateId)
  },
  duration: 10000 // 10 секунд на экране
})
```

---

## 🎬 АНИМАЦИЯ ПЕРЕХОДОВ

### При автопереходе:
1. Карточка подсвечивается (border: yellow, 2s)
2. Плавно перемещается в новую колонку (transition: transform 500ms)
3. Badge меняется с fade эффектом
4. Показывается toast с пояснением

### При действии HR:
1. Кнопка показывает spinner
2. После успеха - галочка (check mark)
3. Карточка обновляется (новые кнопки появляются)
4. Toast с подтверждением

---

**Этот план полностью решает все озвученные проблемы и создает целостный user experience!**